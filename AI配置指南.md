# 灵境小智 AI 配置指南

这份指南用于在**一套正在运行的后端**上启用课程 AI 助手。管理员设置一次后，访问同一套服务的学生无需各自填写 API Key。

## 1. 准备后端加密密钥

打开项目的 `backend/.env`。如果没有这个文件，先把 `backend/.env.example` 复制为 `backend/.env`。

在终端运行下面的命令，生成一串 64 位十六进制字符：

```powershell
node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
```

在 `backend/.env` 增加一行，把生成的字符填在等号后面：

```dotenv
AI_CONFIG_SECRET=这里填写刚生成的64位字符
```

保存后**重启后端**。这串密钥用于加密数据库中的 DeepSeek API Key；它不是 DeepSeek API Key。以后重启服务仍要保留同一串密钥，否则已保存的 API Key 无法解密，需要重新录入。

## 2. 管理员启用助手

1. 以管理员身份登录，进入侧边栏的 **AI 配置**。
2. 填写自己的 **DeepSeek API Key**；模型名称填 `deepseek-flash`，API Base URL 填 `https://api.deepseek.com`。
3. 保留默认系统 Prompt，按需选择“检索上传的课程资料”“显示参考资料”和拓展程度。
4. 打开 **启用 AI 助手**，点击**保存配置**。

API Key 保存后不会在页面回显；以后打开配置页看到“已配置”即可，留空保存不会清除它。退出账号、重新登录或重启服务，通常都不需要再次设置。若 Key 被撤销、过期或额度不足，则需在这里更换。

## 3. 加入课程资料并测试

- 管理员或负责课程的执行导师：进入**课程详情 → 管理 AI 知识库**，上传资料，等待状态显示**可检索**。
- 可检索的文件：有文字内容的 PDF、DOCX、PPTX、TXT。旧版 DOC/PPT 先转换格式；扫描版 PDF 先做 OCR。**只有资料名称、没有实际文件时，不能测试文件检索。**
- 学生：从侧边栏进入**灵境小智**，选择一门自己已报名且已发布的课程，再提问。课程详情页的入口会自动选中当前课程。
- 建议先问“这门课程有哪些任务？”，再问一条只在上传文件中出现的内容，并检查回答中的参考资料。

学生只能访问自己有效报名且已发布的课程。教师和新媒体账号不能使用该助手。

## 4. `localhost` 为什么不能共享数据？

`localhost` 永远指向**当前这台电脑**。如果甲、乙分别在自己的电脑运行项目，他们打开的虽然都是 `localhost`，实际连接的是两套不同的后端、数据库和上传文件目录。

| 使用方式 | 课程数据和 AI 配置是否共享？ |
| --- | --- |
| 同一台电脑上的不同账号，访问同一个本地后端 | 共享；管理员配置一次 Key 即可 |
| 不同电脑各自运行 `localhost` | 不共享；各自有自己的数据库、文件和配置 |
| 不同用户访问同一套已部署的服务 | 共享同一套数据库和 AI 配置；仍按账号与课程权限隔离 |

Git 分支只同步代码。`backend/.env`、SQLite 数据库和上传的课程文件不会随分支提交，所以别人检出这个分支后不会自动得到你的课程数据或 API Key。AI 对话记录也只保留在当前浏览器页面，刷新页面后不会恢复。

如果希望不同电脑上的师生使用**同一批课程资料和同一个 AI 配置**，需要部署一套大家都能访问的服务，并让后端使用持久保存的数据库、上传目录和 `AI_CONFIG_SECRET`。具体部署流程见 [README 的服务器部署章节](README.md#服务器部署)。不要通过把 API Key 写进 Git 分支来实现共享。
