# 数据库初始化修复批次（2026-09-09）

## 问题与验收

- P1 DB-01：测试/正式初始化直接执行最新 schema，却未记录迁移版本，首次启动重复添加 status。改为统一调用现有迁移器；验收为初始化后可重复启动，真实登录成功。
- P2 DB-02：测试初始化忽略 DB_PATH，日志提示内部用户名。与后端路径配置保持一致，提示中文登录姓名；验收为隔离数据库初始化和四类账号登录通过。

## 决策与红线

本批由用户授权修复、补测试、同步 README，并授权在 yyy 完成本地提交。yyy 从本次修复版本创建，不使用旧 origin/yyy 的缺陷代码；xzx-2 恢复到误提交前的 08ea0c2。不推送或部署。不含任何缺陷分支代码移植，不修改权限或数据模型，不新增结构迁移。新库复用 runMigrations；既有库不得盲目标记全部迁移。当前故障库先做 SQLite backup 与完整性检查，逐项比较规范结构后才补记录，保留数据。

## 文件与预写提交信息

1. backend/database/init.js、backend/database/provision.js：`fix(db): 修复初始化与版本迁移衔接`
2. backend/test/databaseInit.test.js：`test(backend): 覆盖数据库初始化与启动回归`
3. README.md、本计划：`docs(readme): 补充数据库初始化与故障恢复说明`

## 执行与回归清单

- [x] 统一新库迁移入口、DB_PATH 与登录提示。
- [x] 测试初始化后两次加载后端数据库，四类默认账号经 HTTP 登录成功。
- [x] 已有库拒绝初始化且保留数据；显式重置后仍可启动。
- [x] 生产环境拒绝测试初始化/强制重置。
- [x] 正式初始化重复运行，只创建一个正式管理员，无测试学校。
- [x] 当前故障库：52 个结构对象与规范完全一致；一致性备份校验通过，补齐版本记录，保留 6 个用户。
- [x] 后端 69 项测试全部通过，前端 lint 与构建通过（构建有超过 500 kB 的资源包提示）；git diff --check 通过。
- [x] 当前故障库修复后实际启动：健康检查 200，管理员姓名登录 200。
- [x] 在本地 yyy 按预写信息分步提交修复、测试及文档。
- [ ] PR/CI、人工审查及测试站回归属于后续合并门禁，本批不执行推送或发布。

Windows 当前库备份使用 better-sqlite3 的 SQLite backup API 与 PRAGMA integrity_check，等效执行仓库 Bash 备份脚本的一致性备份和校验，不复制运行中的裸数据库文件。
