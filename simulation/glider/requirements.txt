# 物理后端所需的绘图/数值依赖（reference 后端 + 3D 可视化）
# 若在 Linux 环境使用 novaPhy 后端，另需按 simulation/README.md 安装 novaphy wheel（不在 PyPI）。
numpy>=1.24
matplotlib>=3.7
Pillow>=9.0
# MP4 回放（--video）所需
imageio>=2.31
imageio-ffmpeg>=0.4.9
# OpenGL 延迟渲染途径（--renderer gl）所需；缺失时自动回退 matplotlib，不影响默认路径
moderngl>=5.10
trimesh>=4.0        # GLB 模型加载
pyglm               # 渲染数学（pyGLM）
